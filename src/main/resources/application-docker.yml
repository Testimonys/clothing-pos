# ============================================
# Docker 环境配置
# 数据库由 MySQL 容器初始化（schema.sql + data.sql），
# Spring Boot 不再执行 SQL 初始化脚本
# ============================================
spring:
  sql:
    init:
      mode: never

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
